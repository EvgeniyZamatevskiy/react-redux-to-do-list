import { TasksSliceInitialStateType } from './types'
import tasksSlice from 'redux/tasks/slice'

let startState: TasksSliceInitialStateType

beforeEach(() => {
	startState = {
		tasks: {}
	}
})

test('a', (() => {

}))
